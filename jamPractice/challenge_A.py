#part used for reading input to variables
with open("file.txt") as fp: #need to edit in filename
    T = fp.readline()
    N = fp.readline()
    stopsDict{}
    while N>0:
        stopsDict[]=
    P =
    interestingList = []
    while P > 0:
        interestingList = interestingList.append([#insert input here])
        P--
